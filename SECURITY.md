# Security

This project has not received a formal security audit.

- Do not store secrets, tokens, API keys, or credentials in this repository.
- Review scripts before running them.
- Install scripts do not create `.env` files and do not modify `config.toml`.
- Report vulnerabilities through GitHub issues unless private reporting is configured.
- Enterprise users should review workflow rules, agent permissions, and install paths before adoption.
